spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/aivlebig?autoReconnect=true&characterEncoding=UTF-8&allowMultiQueries=true&useAffectedRows=true
    username: root
    password: MySQL123!

# XML파일의 <mapper namespace= ""> 로도 연결은 되어있지만
# XML 파일 위치 명시적으로 지정, 향후 XML파일 위치 변경이나 추가 시 유연하게 대응하기 위해
#mybatis:
#  mapper-locations: classpath:mapper/**/*.xml
#  type-aliases-package: kt.aivle.member.model.entity
#  configuration:
#    map-underscore-to-camel-case: true    # snake_case를 camelCase로 자동 매핑


secret:
  access: acc
  refresh: ref